<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Wireframe → Rendered | Parallax Demo</title>
<style>
  * { box-sizing: border-box; }

  html {
    scroll-behavior: smooth;
    background: #0b0b0d;
  }

  body {
    margin: 0;
    background: #0b0b0d;
    color: #fff;
    font-family: Arial, Helvetica, sans-serif;
  }

  .intro,
  .outro {
    min-height: 100vh;
    display: grid;
    place-items: center;
    padding: 8vw;
  }

  .intro-inner,
  .outro-inner {
    max-width: 900px;
    text-align: center;
  }

  .eyebrow {
    text-transform: uppercase;
    letter-spacing: .18em;
    font-size: 12px;
    opacity: .55;
    margin-bottom: 20px;
  }

  h1 {
    font-size: clamp(42px, 7vw, 100px);
    line-height: .95;
    margin: 0 0 24px;
    font-weight: 500;
  }

  p {
    font-size: clamp(17px, 2vw, 24px);
    line-height: 1.5;
    opacity: .65;
    max-width: 700px;
    margin: 0 auto;
  }

  /* The long scrolling area controls the transition. */
  .model-transition {
    height: 220vh;
    position: relative;
  }

  .model-sticky {
    position: sticky;
    top: 0;
    height: 100vh;
    overflow: hidden;
    background: #111;
  }

  .model-image {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    will-change: opacity, transform;
  }

  .wireframe {
    z-index: 1;
  }

  .rendered {
    z-index: 2;
    opacity: 0;
  }

  /* Subtle zoom gives the section a little more depth. */
  .wireframe {
    transform: scale(1);
  }

  .rendered {
    transform: scale(1.04);
  }

  .scrim {
    position: absolute;
    inset: 0;
    z-index: 3;
    pointer-events: none;
    background:
      linear-gradient(to bottom, rgba(0,0,0,.35), transparent 30%, transparent 65%, rgba(0,0,0,.55));
  }

  .caption {
    position: absolute;
    z-index: 4;
    left: 5vw;
    bottom: 6vh;
    max-width: 600px;
    pointer-events: none;
  }

  .caption-label {
    text-transform: uppercase;
    letter-spacing: .18em;
    font-size: 11px;
    margin-bottom: 12px;
    opacity: .65;
  }

  .caption h2 {
    font-size: clamp(30px, 4vw, 60px);
    line-height: 1;
    font-weight: 400;
    margin: 0 0 12px;
  }

  .caption p {
    margin: 0;
    font-size: 16px;
    max-width: 500px;
    color: #fff;
  }

  .progress {
    position: absolute;
    z-index: 5;
    right: 5vw;
    bottom: 6vh;
    width: 130px;
    height: 2px;
    background: rgba(255,255,255,.25);
  }

  .progress-bar {
    height: 100%;
    width: 0%;
    background: #fff;
    transform-origin: left;
  }

  .instructions {
    position: absolute;
    z-index: 5;
    top: 5vh;
    left: 50%;
    transform: translateX(-50%);
    text-transform: uppercase;
    letter-spacing: .14em;
    font-size: 10px;
    opacity: .55;
    white-space: nowrap;
  }

  @media (max-width: 700px) {
    .model-transition { height: 180vh; }
    .caption { left: 7vw; right: 7vw; }
    .progress { left: 7vw; right: auto; bottom: 4vh; width: 100px; }
    .instructions { display: none; }
  }
</style>
</head>

<body>

<section class="intro">
  <div class="intro-inner">
    <div class="eyebrow">3D Reconstruction / Parallax Experiment</div>
    <h1>From Wireframe<br>to Reality</h1>
    <p>
      Scroll slowly. The technical model gradually transforms into the finished
      visualization.
    </p>
  </div>
</section>

<section class="model-transition" id="modelTransition">

  <div class="model-sticky">

    <img
      src="images/render_1_a.jpg"
      alt="3D model wireframe"
      class="model-image wireframe"
    >

    <img
      src="images/render_1_b.jpg"
      alt="Fully rendered 3D model"
      class="model-image rendered"
    >

    <div class="scrim"></div>

    <div class="instructions">Scroll to transform</div>

    <div class="caption">
      <div class="caption-label" id="captionLabel">01 / Wireframe</div>
      <h2 id="captionTitle">The structure</h2>
      <p id="captionText">
        Geometry, proportions and spatial relationships are established first.
      </p>
    </div>

    <div class="progress">
      <div class="progress-bar" id="progressBar"></div>
    </div>

  </div>

</section>

<section class="outro">
  <div class="outro-inner">
    <div class="eyebrow">Visualization</div>
    <h1>The finished model.</h1>
    <p>
      This section can lead into the rest of the project, project description,
      methodology, GIS map, or another interactive scene.
    </p>
  </div>
</section>

<script>
  const section = document.getElementById("modelTransition");
  const rendered = document.querySelector(".rendered");
  const wireframe = document.querySelector(".wireframe");
  const progressBar = document.getElementById("progressBar");

  const captionLabel = document.getElementById("captionLabel");
  const captionTitle = document.getElementById("captionTitle");
  const captionText = document.getElementById("captionText");

  function clamp(value, min, max) {
    return Math.min(Math.max(value, min), max);
  }

  function easeInOut(t) {
    return t < 0.5
      ? 2 * t * t
      : 1 - Math.pow(-2 * t + 2, 2) / 2;
  }

  function update() {
    const rect = section.getBoundingClientRect();
    const scrollableDistance = section.offsetHeight - window.innerHeight;

    let progress = -rect.top / scrollableDistance;
    progress = clamp(progress, 0, 1);

    // Smooth transition curve.
    const fade = easeInOut(progress);

    rendered.style.opacity = fade;
    progressBar.style.width = `${progress * 100}%`;

    // Very subtle opposing movement.
    wireframe.style.transform = `scale(${1 + progress * 0.025})`;
    rendered.style.transform = `scale(${1.04 - progress * 0.04})`;

    // Change the caption as the transformation progresses.
    if (progress < 0.35) {
      captionLabel.textContent = "01 / Wireframe";
      captionTitle.textContent = "The structure";
      captionText.textContent =
        "Geometry, proportions and spatial relationships are established first.";
    } else if (progress < 0.72) {
      captionLabel.textContent = "02 / Visualization";
      captionTitle.textContent = "Bringing it to life";
      captionText.textContent =
        "Materials, lighting and context begin to reveal the finished space.";
    } else {
      captionLabel.textContent = "03 / Final Model";
      captionTitle.textContent = "The finished visualization";
      captionText.textContent =
        "The technical model becomes a visual representation of the completed reconstruction.";
    }
  }

  window.addEventListener("scroll", update, { passive: true });
  window.addEventListener("resize", update);
  update();
</script>

</body>
</html>
